import React from "react";

const AboutPage = () => (
  <div>
    <h2>About</h2>
    <p>
      This app uses React, React router, Redux, any many other React libraries
    </p>
  </div>
);

export default AboutPage;
